name: Docker Build

on:
  push:
    paths:
      - 'images/services/**'

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log into registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Detect changed services
        id: changes
        run: |
          echo "CHANGED_SERVICES=$(git diff --name-only ${{ github.sha }} ${{ github.event.before }} | grep -oP 'images/services/\K[^/]+(?=/)' | sort -u | tr '\n' ' ')" >> $GITHUB_ENV

      - name: Build and push changed images
        if: ${{ env.CHANGED_SERVICES != '' }}
        uses: docker/build-push-action@v6
        with:
          context: images/services/${{ env.CHANGED_SERVICES }}
          push: true
          tags: ${{ env.REGISTRY }}/myrenic/${{ env.CHANGED_SERVICES }}:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

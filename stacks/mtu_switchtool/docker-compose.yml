version: "3.9"

services:
  db:
    image: postgres:latest
    container_name: mtu_switchtool_db
    restart: always
    environment:
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    volumes:
      - database:/var/lib/postgresql/data
    networks:
      - mtu_switchtool-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      retries: 5

  frontend:
    image: ghcr.io/myrenic/switchreporttool:frontend-latest
    restart: unless-stopped
    environment:
      NODE_ENV: production
      VITE_REACT_APP_DATABASE_API_URL: ${VITE_REACT_APP_DATABASE_API_URL}
      VITE_REACT_APP_CISCO_API_URL: ${VITE_REACT_APP_CISCO_API_URL}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.frontend.rule=PathPrefix(`/mtu`)"
      - "traefik.http.routers.frontend.entrypoints=web"
      - "traefik.http.services.frontend.loadbalancer.server.port=80"
    networks:
      - mtu_switchtool-network
      - web
    depends_on:
      - backend-db
      - backend-cisco

  backend-cisco:
    image: ghcr.io/myrenic/switchreporttool:backend-cisco-latest  
    restart: unless-stopped
    environment:
      API_TO_DB: http://backend-db:5000
      HOST_USERNAME: ${HOST_USERNAME}
      HOST_PASSWORD: ${HOST_PASSWORD}
      API_PASSWORD: ${CISCO_API_PASSWORD}
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend-cisco.rule=PathPrefix(`/api/cisco`)"
      - "traefik.http.routers.backend-cisco.entrypoints=web"
      - "traefik.http.services.backend-cisco.loadbalancer.server.port=5001"
    networks:
      - mtu_switchtool-network

  backend-db:
    image: ghcr.io/myrenic/switchreporttool:backend-db-latest  
    restart: unless-stopped
    environment:
      DB_PASSWORD: ${DB_PASSWORD}
      DB_HOST: mtu_switchtool_db
      DB_PORT: 5432
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.backend-db.rule=PathPrefix(`/api/db`)"
      - "traefik.http.routers.backend-db.entrypoints=web"
      - "traefik.http.services.backend-db.loadbalancer.server.port=5000"
    networks:
      - mtu_switchtool-network

volumes:
  database:

networks:
  mtu_switchtool-network:
    driver: bridge
  web: